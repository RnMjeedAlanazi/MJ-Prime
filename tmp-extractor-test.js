const axios = require('axios');

async function testFetch() {
    try {
        const token = '1234'; // We need a real token for testing, but let's just see if we can reach it
        // Actually, without a real token, the page might just return invalid token. 
        // I should just read how faselhd player works from internet or prior knowledge.
        
    } catch (e) {
        console.error(e);
    }
}
testFetch();
